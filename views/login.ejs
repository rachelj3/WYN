<!DOCTYPE html>
<html lang="en-US">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>WYN: Login</title>
        <link rel="stylesheet" href="stylesheets/WYN.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
    </head>

    <body>
        <container>
            
            <%- include("header")%>

            <main>
                <loginBG>
                    <loginContainer class="centering">
                        <div>
                            <loginTitle>
                                login
                            </loginTitle>
                            <form class="loginForm" action="/login" method="POST">
                                <% if (typeof message !== 'undefined') { %>
                                    <span class = "loginWarn"><%=message %><br></span>
                                <% } %>
                                
                                <label for="email">email</label>
                                <input class="loginField" type="email" id="email" name="email">
                                <label for="pwd">password</label>
                                <input class="loginField" type="password" id="pwd" name="pwd">
                                <a href="profile.ejs"><input class="blueButton centering" style="margin-top:12px;" type="submit" value="Submit"login></a>
                            </form>
                            <div class="centering">Don't have an account?</div>
                            <a class="centering loginLink" href="signup.ejs">sign up!</a>
                        </div>
                    </loginContainer>
                </loginBG>
            </main>


            <footer>
                <div class="flex-box-row" id="footer-div">
                    <div class="flex-item">
                        <h3><a class="underlineLink" href="/">home</a></h3>
                    </div>
                    <div class="flex-item">
                        <img src="images/main-logo-white.png">
                    </div>
                    <div class="flex-item">
                        <h3><a class="underlineLink" href="aboutUs.ejs">about us</a></h3>
                    </div>
                </div>
            </footer>

        </container>
    </body>
</html>